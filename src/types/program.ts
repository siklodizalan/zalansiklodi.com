export type CardStyle = {
  images: string[];
  outerBg: string;
  outerBorder: string;
  innerBg: string;
  innerBorder: string;
};

export type Program = {
    title: string
    price: string
    specs: string[]
};